---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contacto | E2V Bolivia">
  <main class="contact-page">
    
    <section id="contacto" class="section-contact">
      <div class="container relative">
        <div class="contact-header">
          <span class="eyebrow fade-in">Conectemos</span>
          <h1 class="section-title fade-in delay-1">Hablemos de tu proyecto</h1>
          <p class="fade-in delay-2">Estamos listos para ofrecerte la mejor asesoría técnica y soluciones energéticas reales para tu empresa.</p>
        </div>

        <div class="contact-grid">
          
          <div class="contact-info fade-in delay-2">
            
            <div class="info-item highlight interactive">
              <div class="info-icon whatsapp">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
              </div>
              <div>
                <h3>WhatsApp (Principal)</h3>
                <a href="https://wa.me/591XXXXXXX" target="_blank" class="contact-link">+591 XXXXXXXX</a>
              </div>
            </div>

            <div class="info-item interactive">
              <div class="info-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
              </div>
              <div>
                <h3>Email</h3>
                <a href="mailto:info@e2v.bo" class="contact-link">info@e2v.bo</a>
              </div>
            </div>

            <div class="info-item interactive">
              <div class="info-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
              </div>
              <div>
                <h3>Teléfono Oficina</h3>
                <span class="contact-link">+591 3 3543000</span>
              </div>
            </div>
            
          </div>

          <form class="contact-form fade-in delay-3">
            <div class="form-header">
              <h3>Envíanos un mensaje</h3>
              <p>Te responderemos a la brevedad posible.</p>
            </div>

            <div class="form-row">
              <div class="form-group">
                <label for="nombre">Nombre Completo</label>
                <input type="text" id="nombre" name="nombre" placeholder="Ej. Juan Pérez" required />
              </div>
              <div class="form-group">
                <label for="empresa">Empresa</label>
                <input type="text" id="empresa" name="empresa" placeholder="Tu organización" />
              </div>
            </div>

            <div class="form-row">
              <div class="form-group">
                <label for="telefono">Teléfono</label>
                <input type="tel" id="telefono" name="telefono" placeholder="+591" required />
              </div>
              <div class="form-group">
                <label for="email">Email corporativo</label>
                <input type="email" id="email" name="email" placeholder="correo@ejemplo.com" required />
              </div>
            </div>

            <div class="form-group">
              <label for="mensaje">Detalles del Proyecto</label>
              <textarea id="mensaje" name="mensaje" rows="4" placeholder="Describe brevemente tu requerimiento..." required></textarea>
            </div>

            <button type="submit" class="btn-primary-form">Enviar Solicitud</button>
          </form>
          
        </div>
      </div>
    </section>

  </main>
</Layout>

<style>
  /* --- Estructura Base --- */
  .contact-page { background-color:  var(--white); min-height: 100vh; }
  .container { max-width: var(--content-max-width, 1200px); margin: 0 auto; padding: 10 1.5rem; }
  .relative { position: relative; z-index: 2; }
  
  :root { --radius-sm: 6px; --radius-md: 12px; }

  /* --- Sección --- */
  .section-contact { padding: 20px 0; background: var(--white); position: relative; overflow: hidden; }

  /* --- Encabezado --- */
  .contact-header { text-align: center;margin: 0 auto 4rem; }
.eyebrow { 
    color: var(--cta-dark); 
    background-color: var(--cta-main); 
    padding: 0.35rem 1rem; 
    font-weight: 700; 
    font-size: 0.8rem; 
    text-transform: uppercase; 
    letter-spacing: 0.05em; 
    display: inline-block; 
    
    /* AQUÍ ESTÁ EL CAMBIO: Redujimos el margen de 1rem a 0.3rem */
    margin-bottom: 0.3rem; 
    border-radius: 50px; 
  }  
.section-title { 
    font-size: clamp(2.5rem, 5vw, 4rem); 
    color: var(--brand-800); 
    
    /* AQUÍ ESTÁ EL CAMBIO: Forzamos a que el margen superior sea 0 */
    margin-top: 0; 
    margin-bottom: 1rem; 
    
    font-family: var(--font-header); 
    text-transform: uppercase; 
    line-height: 1.1; 
  }
    .contact-header p { font-size: 1.15rem; color: var(--brand-600); line-height: 1.6; margin: 0; }

  /* --- Grid Principal --- */
  .contact-grid { display: grid; grid-template-columns: 0.85fr 1.15fr; gap: 4rem; align-items: stretch; }

  /* --- Información de Contacto (Tarjetas) --- */
  .contact-info { display: flex; flex-direction: column; gap: 1.2rem; }
  
  .info-item {
    display: flex; gap: 1.2rem; align-items: center; padding: 1.5rem;
    background: var(--brand-100); border: 1px solid transparent;
    border-radius: var(--radius-md);
    transition: all 0.2s ease;
  }
  
  .info-item.interactive:hover { border-color: var(--brand-200); transform: translateX(5px); }

  .info-item.highlight {
    background: var(--brand-800); color: var(--white); 
    box-shadow: 0 10px 25px rgba(0,0,0,0.1);
  }
  .info-item.highlight.interactive:hover { transform: translateX(5px); box-shadow: 0 15px 35px rgba(0,0,0,0.15); }
  .info-item.highlight .contact-link { color: var(--white); }

  .info-icon {
    width: 48px; height: 48px; background: var(--white); color: var(--brand-600);
    display: flex; align-items: center; justify-content: center;
    border-radius: 50%; flex-shrink: 0; border: 1px solid var(--brand-200);
  }
  .info-item.highlight .info-icon { background: var(--cta-main); color: var(--brand-800); border: none; }

  .info-item h3 { font-size: 0.85rem; text-transform: uppercase; margin-bottom: 0.3rem; color: var(--brand-600); }
  .info-item.highlight h3 { color: var(--brand-400); }
  
  .contact-link { text-decoration: none; font-weight: 700; font-size: 1.2rem; color: var(--brand-800); transition: color 0.2s; }
  .contact-link:hover { color: var(--cta-main); }

  /* --- Formulario Limpio (Estilo SaaS) --- */
/* --- Formulario Limpio (Estilo SaaS) --- */
  .contact-form {
    display: flex; flex-direction: column; gap: 1.5rem;
    background: var(--white); 
    
    /* AQUÍ ESTÁ EL CAMBIO: padding de (Arriba) (Derecha) (Abajo) (Izquierda) 
       Redujimos el de arriba de 3rem a 1.8rem */
    padding: 1.8rem 3rem 3rem 3rem; 
    
    border: 1px solid rgba(0,0,0,0.05);
    border-radius: var(--radius-md);
    box-shadow: 0 15px 50px rgba(0,0,0,0.04); 
  }

  .form-header h3 { 
    font-size: 1.8rem; 
    color: var(--brand-800); 
    
    /* AQUÍ ESTÁ EL CAMBIO: Forzamos a cero el margen de arriba */
    margin-top: 0; 
    margin-bottom: 0.3rem; 
  }
  .form-header p { color: var(--brand-600); margin-bottom: 0; font-size: 1rem; }

  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
  .form-group { display: flex; flex-direction: column; gap: 0.5rem; }

  .form-group label {
    font-size: 0.8rem; font-weight: 700; text-transform: uppercase; color: var(--brand-800);
  }

  /* Inputs Suavizados */
  .form-group input, .form-group textarea {
    padding: 1rem 1.2rem; border: 1px solid var(--brand-200); border-radius: var(--radius-sm);
    background: var(--brand-100); font-family: inherit; font-size: 1rem; color: var(--brand-800);
    transition: all 0.2s ease;
  }
  
  /* Resplandor naranja en lugar de sombra dura al escribir */
  .form-group input:focus, .form-group textarea:focus {
    outline: none; border-color: var(--cta-main); background: var(--white);
    box-shadow: 0 0 0 4px rgba(254, 183, 64, 0.15);
  }

  .btn-primary-form {
    background: var(--brand-800); color: var(--white); border: none; padding: 1.2rem;
    font-weight: 700; font-size: 1.1rem; text-transform: uppercase; letter-spacing: 0.05em;
    cursor: pointer; transition: all 0.2s; margin-top: 0.5rem; border-radius: var(--radius-sm);
  }
  .btn-primary-form:hover { background: var(--cta-main); color: var(--brand-800); transform: translateY(-2px); box-shadow: 0 8px 20px rgba(254, 183, 64, 0.2); }
  .btn-primary-form:active { transform: translateY(0); box-shadow: none; }

  /* --- Animaciones Keyframes --- */
  .fade-in { opacity: 0; animation: fadeIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
  .delay-1 { animation-delay: 0.1s; }
  .delay-2 { animation-delay: 0.2s; }
  .delay-3 { animation-delay: 0.3s; }

  @keyframes fadeIn { 
    from { opacity: 0; transform: translateY(20px); } 
    to { opacity: 1; transform: translateY(0); } 
  }

  /* --- Responsive --- */
  @media (max-width: 900px) {
    .section-contact { padding: 3rem 0; }
    .contact-grid { grid-template-columns: 1fr; gap: 3rem; }
    .form-row { grid-template-columns: 1fr; gap: 1.5rem; }
    .contact-form { padding: 2rem 1.5rem; }
  }
</style>