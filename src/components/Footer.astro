---
import { Image } from 'astro:assets';
import e2vLogo from '../assets/E2V.png';
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer-top-bar"></div>

  <div class="footer-container">
    
    <div class="footer-brand">
      <div class="logo-wrapper">
        <Image src={e2vLogo} alt="E2V Logo" class="footer-logo" quality="high"/>
      </div>
      <p>Energía inteligente y soluciones eléctricas de alta precisión. Innovando para el futuro energético de Bolivia.</p>
      
      <div class="social-links">
        <a href="https://wa.me/tu-numero" class="social-icon" aria-label="WhatsApp">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
        </a>
        <a href="https://facebook.com" class="social-icon" aria-label="Facebook">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path></svg>
        </a>
        <a href="https://instagram.com" class="social-icon" aria-label="Instagram">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
        </a>
        <a href="https://linkedin.com" class="social-icon" aria-label="LinkedIn">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"></path><rect x="2" y="9" width="4" height="12"></rect><circle cx="4" cy="4" r="2"></circle></svg>
        </a>
      </div>
    </div>

    <div class="footer-links">
      <h3>Navegación</h3>
      <ul>
        <li><a href="/"><span>Inicio</span></a></li>
        <li><a href="/quienes-somos"><span>Quiénes Somos</span></a></li>
        <li><a href="/noticias"><span>Noticias</span></a></li>
        <li><a href="/" class="highlight-link"><span>⚡ ElectroPoint</span></a></li>
      </ul>
    </div>

    <div class="footer-contact">
      <h3>Contacto</h3>
      <ul class="contact-list">
        <li>
          <span class="icon">📍</span>
          <p>Santa Cruz de la Sierra, Bolivia</p>
        </li>
        <li>
          <span class="icon">✉️</span>
          <a href="mailto:info@e2v.bo">info@e2v.bo</a>
        </li>
        <li>
          <span class="icon">📞</span>
          <a href="tel:+59133543000">+591 3 3543000</a>
        </li>
      </ul>
    </div>
  </div>

  <div class="footer-bottom">
    <div class="container-bottom">
      <p>&copy; {currentYear} E2V. Todos los derechos reservados.</p>
      <div class="legal-links">
        <a href="#">Privacidad</a>
        <a href="#">Términos Legales</a>
      </div>
    </div>
  </div>
</footer>

<style>
  /* --- Contenedor Principal --- */
  .footer {
    background-color: var(--brand-800);
    color: var(--brand-100);
    position: relative;
    font-family: var(--font-main);
    border-top: 1px solid var(--brand-600);
  }

  /* Adorno técnico superior */
  .footer-top-bar {
    height: 4px;
    width: 100%;
    background: linear-gradient(90deg, var(--brand-800) 0%, var(--cta-main) 50%, var(--brand-800) 100%);
  }

  .footer-container {
    width: 90%;
    max-width: 1200px;
    margin: auto;
    display: grid;
    grid-template-columns: 1.5fr 1fr 1fr; /* Le da más espacio a la descripción */
    gap: 4rem;
    padding: 5rem 0 4rem 0;
  }

  /* --- Marca y Logo --- */
  .logo-wrapper {
    display: inline-block;
    padding-bottom: 1rem;
    margin-bottom: 1.5rem;
    border-bottom: 2px solid var(--cta-main);
  }

  .footer-logo {
    height: 65px; 
    width: auto;
    mix-blend-mode: screen; 
    filter: brightness(0) invert(1);
    display: block;
  }

  .footer-brand p {
    color: var(--brand-200);
    line-height: 1.6;
    max-width: 320px;
    font-size: 0.95rem;
    margin-bottom: 2rem;
  }

  /* --- Redes Sociales (Estilo Técnico) --- */
  .social-links {
    display: flex;
    gap: 12px;
  }

  .social-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background-color: var(--brand-1000, #0f141e);
    color: var(--cta-main);
    border: 1px solid var(--brand-600);
    border-radius: 4px;
    transition: all 0.2s ease;
  }

  .social-icon:hover {
    background-color: var(--cta-main);
    color: var(--brand-800);
    transform: translateY(-3px);
    box-shadow: 4px 4px 0 var(--white);
    border-color: var(--cta-main);
  }

  /* --- Títulos de Columnas --- */
  h3 {
    font-family: var(--font-header);
    color: var(--white);
    font-size: 1.1rem;
    letter-spacing: 0.05em;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  h3::before {
    content: '';
    display: block;
    width: 12px;
    height: 12px;
    background-color: var(--cta-main);
  }

  /* --- Enlaces de Navegación --- */
  .footer-links ul { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 1rem; }

  .footer-links a {
    color: var(--brand-200);
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
  }

  /* Efecto flecha al hacer hover */
  .footer-links a::before {
    content: '›';
    color: var(--cta-main);
    font-weight: bold;
    font-size: 1.2rem;
    margin-right: 0;
    opacity: 0;
    transform: translateX(-10px);
    transition: all 0.2s ease;
  }

  .footer-links a:hover { color: var(--white); transform: translateX(5px); }
  .footer-links a:hover::before { opacity: 1; margin-right: 8px; transform: translateX(0); }

  .highlight-link span { color: var(--cta-main); font-weight: 700; }

  /* --- Lista de Contacto --- */
  .contact-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 1.2rem; }
  .contact-list li { display: flex; gap: 10px; align-items: flex-start; }
  .contact-list .icon { font-size: 1.1rem; filter: grayscale(100%) brightness(200%); /* Hace los emojis grises/blancos para no romper la estética */ opacity: 0.7; }
  
  .contact-list p, .contact-list a { color: var(--brand-200); font-size: 0.95rem; text-decoration: none; transition: color 0.2s; margin: 0; line-height: 1.4; }
  .contact-list a:hover { color: var(--cta-main); }

  /* --- Barra Inferior --- */
  .footer-bottom {
    border-top: 1px solid rgba(255,255,255,0.05);
    background: var(--brand-1000, #0f141e); /* Un color un poco más oscuro que el fondo principal */
    padding: 1.5rem 0;
  }

  .container-bottom {
    width: 90%;
    max-width: 1200px;
    margin: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.85rem;
    color: var(--brand-400);
  }

  .container-bottom p { margin: 0; }

  .legal-links { display: flex; gap: 1.5rem; }
  .legal-links a {
    color: var(--brand-400);
    text-decoration: none;
    transition: color 0.2s;
  }
  .legal-links a:hover { color: var(--white); }

  /* --- Responsive --- */
  @media (max-width: 900px) {
    .footer-container {
      grid-template-columns: 1fr;
      gap: 3rem;
      padding: 4rem 0 3rem 0;
    }

    .footer-brand p { max-width: 100%; }
    
    .container-bottom {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
    }
  }
</style>